import React, { useState } from 'react';
import { useQuery, GET_ONE } from '@stbui/prophet-core';
import { Row, Col, Spin, Card, Form, Radio } from 'antd';

export default () => {
    return <div>1</div>;
};
