import { ReactNode } from 'react';

export type CustomRoutesProps = {
    children: ReactNode;
    noLayout?: boolean;
};

export const CustomRoutes = (props: CustomRoutesProps) => {
    return null;
};

CustomRoutes.prophetName = 'CustomRoutes';
